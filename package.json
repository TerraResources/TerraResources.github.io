{
  "name": "terraresources-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:downloads && astro dev",
    "start": "npm run build:downloads && astro dev",
    "check": "astro check",
    "build:downloads": "node scripts/build-downloads.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:content": "node scripts/check-content.mjs",
    "build": "npm run build:downloads && astro check && astro build && npm run check:links && npm run check:content",
    "preview": "astro preview"
  },
  "dependencies": {
    "@fontsource-variable/manrope": "^5.3.0",
    "astro": "^7.3.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "jszip": "^3.10.2",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
